{
    "watch": ["src"],
    "ext": "ts",
    "ignore": [],
    "exec": "./build && homebridge --debug --user-storage-path ./storage",
    "signal": "SIGTERM",
    "env": {
        "NODE_OPTIONS": "--trace-warnings"
    }
}
